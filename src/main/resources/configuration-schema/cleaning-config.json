{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Configuration model for cleaners.",
  "type": "object",
  "properties": {
    "fileNameRegex": {
      "type": "string",
      "description": "Regular expression that will be used to match files of a directory."
    },
    "timeUnit": {
      "type": {
        "anyOf": ["days", "hours", "minutes", "seconds"],
        "description": "The time unit of the removeAfter parameter"
      }
    },
    "removeAfter": {
      "type": "integer",
      "description": "The time which files will be preserved from deletion. For example, you may want to delete files which creation time is higher than 1 hour."
    }
  },
  "required": ["timeUnit", "removeAfter"]
}